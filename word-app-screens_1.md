# 単語暗記アプリ 画面構成メモ

これまでの要件確認をもとに、画面構成と主な要素を洗い出したメモです。フラッシュカード機能の追加と、単語リストのイメージ画像を踏まえてデータモデル・画面構成を更新しました。実装前のたたき台として、過不足があれば教えてください。

> サイドバー・上部ヘッダー・ホーム画面・アイコン規則など、画面横断的なGUIルールは `word-app-gui.md` にまとめてあります。本ドキュメントは各画面固有の目的・表示要素・操作を扱います。

## 全体のデータモデル

- **アカウント（Account）**: id, ニックネーム, アイコン種別（`default`未設定／`preset`動物イラスト／`custom`アップロード画像）, アイコン値（presetの場合はイラストのキー、customの場合は圧縮済み画像のdataURL）, 作成日時
- **単語帳（Tanchou）**: id, アカウントID（Accountへの参照）, 名前, **スター（isStarred、真偽値。単語帳一覧カードからON/OFF可能）**, 作成日時
- **単語（Word）**: id, 単語帳ID, 単語, 意味, **補足（note、任意項目・空文字列可）**, 定着度, フラッシュカード出題状況, 4択クイズ出題状況, **スター（isStarred、真偽値）**, 作成日時

### アカウントの考え方

- アプリを初めて開いたときに、ニックネームとアイコンを設定してアカウントを作成する（アカウントidを発行）。以降、単語帳・単語などのデータはこのアカウントに紐づけて保存する。
- **1端末＝1アカウント固定**。アカウントの追加作成や切り替え機能は持たない。一度作成したアカウントは、その端末・ブラウザ内で保存されたデータと結びついたまま使い続ける。
- ニックネーム・アイコンは、初回作成後も設定画面から変更できる。
- アイコンは3パターンから選べる。
  - デフォルト（未設定）のままにする
  - あらかじめ用意された動物イラストから選ぶ（実装時にシンプルなイラスト／絵文字ベースの素材をいくつか用意する想定）
  - カメラロール（端末の画像）からアップロードする。アップロード時は自動的に縮小・圧縮してから保存する（localStorageの容量を圧迫しないため）

### 属性の考え方（単語リストのイメージ画像を反映）

単語リストは「単語」「意味」「補足」「定着度」「フラッシュカード」「4択クイズ」「スター」の7列で、意味・補足以外の属性はそれぞれ独立して管理する。

- **定着度**：`覚えていない` / `少し覚えた` / `覚えた` の3値。フラッシュカード・4択クイズに共通の属性。新規登録時の初期値は`覚えていない`。
- **フラッシュカード出題状況**：`未出題` / `出題済` の2値。新規登録時は`未出題`。フラッシュカードでラベルを1回選択すると`出題済`になる。
- **4択クイズ出題状況**：`未出題` / `出題済` の2値。新規登録時は`未出題`。4択クイズで1回回答すると`出題済`になる。
- **スター**：`スター付き` / `スターなし` の2値。定着度・出題状況とは完全に独立。単語リスト・フラッシュカード出題画面・4択クイズ出題画面から直接ON/OFFできる。出題前フィルタ画面の絞り込み条件としても使える（「スター付きのみ表示」）。
- **補足**：任意の自由記述（空文字列＝補足なし）。定着度・出題状況・スターとは独立し、値を持たない場合も表示・出題に影響しない。

これらの属性は互いに独立しているため、「4択クイズでは出題済で定着度は『少し覚えた』だが、フラッシュカードではまだ未出題」といった状態が起こり得る（単語リストの画像の「レジスタ」の行がこの状態に該当）。

単語リスト画面では、定着度／フラッシュカード出題状況／4択クイズ出題状況／スターをそれぞれ直接プルダウンやトグルボタンで手動変更できる（画像の各列にドロップダウンの矢印がある想定を反映）。補足は一覧では📝/—のアイコン表示のみで、内容の追加・編集は単語登録・編集モーダルから行う。

### 属性の更新ルール

- **4択クイズで回答した時**：状態遷移ルールに従って定着度を更新し、4択クイズ出題状況を`出題済`にする
  - 不正解：現在「覚えた」→「少し覚えた」、それ以外（覚えていない／少し覚えた）→「覚えていない」
  - 正解：現在「少し覚えた」→「覚えた」、それ以外（覚えていない）→「少し覚えた」
  - （2問連続正解で「覚えた」、1回正解で「少し覚えた」、不正解で「覚えていない」になる、というこれまでの自動ラベリングルールと同じ）
- **フラッシュカードの意味画面でラベル（覚えた／少し覚えた／覚えていない）を選択した時**：状態遷移ルールは適用せず、選択したラベルをそのまま定着度に設定し、フラッシュカード出題状況を`出題済`にする。**ラベルを選択すると同時に次の単語へ進む**（選択操作＝次へ進む操作を兼ねる）
- **単語リスト画面から手動で変更した時**：定着度／フラッシュカード出題状況／4択クイズ出題状況／スターのいずれも、選んだ値をそのまま設定する（他の属性には影響しない）
- **単語の編集（単語・意味・補足の修正）**：定着度・出題状況・スターともにリセットせず、そのまま保持する
- **スターの切り替え**：単語リスト・フラッシュカード出題画面・4択クイズ出題画面・単語帳一覧（単語帳自体のスター）のいずれから切り替えても、他の属性には一切影響しない
- データ保存はブラウザ内（localStorage等）のみ

---

## -1. 初回起動時：アカウント作成画面

**目的**：アプリを初めて開いたときに、ニックネームとアイコンを設定してアカウントを作成する

**表示要素**
- ニックネーム入力欄（必須。1〜20文字、絵文字・特殊記号も制限なく使用可）
- アイコン選択（3パターンから選べるUI）
  - デフォルト（未設定）のままにする
  - あらかじめ用意された動物イラストから選ぶ
  - カメラロールから画像をアップロードする（選択後、自動で縮小・圧縮してからプレビュー表示）
- 「はじめる」ボタン

**主な操作**
- ニックネームを入力し、アイコンを選ぶ（アイコンは未設定のままでも進められる）
- 「はじめる」を押すとアカウントが作成され（idを発行）、**チュートリアル・お試し用のデフォルト単語帳「英検３級」（単語100件、意味・品詞の補足つき）が自動生成された上で**、ホーム画面へ遷移する
- 以降、同じ端末・ブラウザでアプリを開いた場合はこの画面を表示せず、ホーム画面から始まる（ローカルにアカウントが存在するかどうかで判定）

---

## 0. ホーム画面（アプリのデフォルト画面）

**目的**：アプリを開いたときに最初に表示する画面。お知らせと使い方の案内をする

**表示要素**
- 「アップデートのお知らせ」：直近の更新点の箇条書き
- 「使い方」：単語帳／フラッシュカード／4択クイズそれぞれの使い方を案内するカード（タップで該当機能へ遷移）

**主な操作**
- 各チュートリアルカードをタップして単語帳一覧・フラッシュカード用単語帳選択・4択クイズ用単語帳選択へ遷移
- サイドバーの他の項目（プロフィール／単語帳／フラッシュカード／4択クイズ）からいつでも移動できる

詳細は `word-app-gui.md` の「3. ホーム画面」を参照。

---

## 0.5. 単語帳選択後のサブメニュー

**目的**：単語帳を選んだ後、行いたい操作（学習方法）を選ぶ

**表示要素**
- 「単語リスト」「フラッシュカード」「4択クイズ」の3項目（この順に上から表示。各項目の左にアイコン付き）
- 4択クイズは、その単語帳の登録単語数（フィルタ前の総数）が4未満の場合は選択不可にする。選択できない旨のメッセージ：「4単語以上単語帳に登録することでこの機能は利用できるようになります。たくさん単語を登録しよう！」

**主な操作**
- 単語リスト → 単語リスト画面へ
- フラッシュカード → 出題前フィルタ・設定画面（フラッシュカード用）へ
- 4択クイズ → 出題前フィルタ・設定画面（4択クイズ用）へ（4語未満の場合は遷移不可、上記メッセージを表示）

サイドバーの「フラッシュカード」「4択クイズ」から単語帳選択画面経由で直接来た場合も、遷移先の出題前フィルタ・設定画面は同じもの（詳細は `word-app-gui.md` の「2. サイドバーのナビゲーション項目」を参照）。

---

## 1. 単語帳一覧画面

**目的**：登録済みの単語帳を一覧表示し、各種操作の入口にする（サイドバーの「単語帳」から遷移。アプリのデフォルト画面ではない。デフォルトは0.のホーム画面）

**表示要素**
- 単語帳カード/リストの一覧（単語帳名、登録単語数、定着度別の内訳、スターのON/OFFボタン）
- 新規単語帳作成ボタン

**主な操作**
- 単語帳の新規作成・名称変更・削除（削除時は確認ダイアログを表示する。新規作成時は現在のアカウントに自動的に紐づく）
  - **新規作成時は、名前を保存すると単語帳一覧に留まらず、作成した単語帳の単語リスト画面へ自動遷移し、単語登録モーダルが開いた状態になる**（1つ目の単語をすぐ入力できる）
- 単語帳のスターをON/OFF（他の属性には影響しない）
- 単語帳を選んでサブメニュー（0.5.）へ遷移

アカウントのニックネーム・アイコン表示とプロフィールへの導線はサイドバー（常設）に移動した。

---

## 1.5. アカウント設定画面

**目的**：作成済みのニックネーム・アイコンを後から変更する（サイドバーの「プロフィール」から遷移）

**表示要素**
- 現在のニックネーム・アイコンの表示
- ニックネーム編集欄
- アイコン再選択（初回のアカウント作成画面と同じ3パターン：デフォルト／動物イラスト／カメラロールアップロード）
- 保存ボタン

**主な操作**
- ニックネーム・アイコンを変更して保存
- 単語帳一覧に戻る

---

## 2. 単語リスト画面

**目的**：特定の単語帳内の単語を管理する

**表示要素**（イメージ画像の列構成を反映）
- 単語・意味・補足・定着度・フラッシュカード（出題済/未出題）・4択クイズ（出題済/未出題）・スターの7列からなる一覧テーブル
  - 補足列は内容を表示せず📝（補足あり）/—（補足なし）のアイコンのみ。ホバーで内容をツールチップ表示
  - 定着度／フラッシュカード／4択クイズの各列はプルダウン、スター列はトグルボタンで直接値を変更できる
- 各属性ごとの件数サマリー（定着度の内訳、フラッシュカード出題済/未出題の内訳、4択クイズ出題済/未出題の内訳、スター付き/スターなしの内訳）
- 単語の新規登録ボタン／CSVインポートボタン
- 各行に削除操作

**主な操作**
- 単語の個別登録（→ 単語登録モーダル/画面。単語名をクリックすると編集モーダルが開く）
- CSVインポート（→ CSVインポートモーダル/画面）
- 単語の編集（単語・意味・補足の修正。定着度・出題状況・スターはリセットせずそのまま保持）
- 定着度／フラッシュカード出題状況／4択クイズ出題状況／スターを一覧から直接手動変更
- 単語の削除

---

## 3. 単語登録・編集モーダル/画面

**目的**：単語と意味を1件ずつ登録・修正する

**表示要素**
- 単語入力欄
- 意味入力欄
- 補足入力欄（複数行、任意項目・空でも保存可）
- 保存／キャンセルボタン

**主な操作**
- 新規登録時：定着度は「覚えていない」、フラッシュカード・4択クイズの出題状況はともに「未出題」、スターは「スターなし」で作成
- 既存単語の編集：内容を保存（定着度・出題状況・スターは変更せずそのまま維持）
- バリデーション：単語・意味が空でないこと（補足は空でも可）、同一単語帳内での重複単語の扱い（上書き確認。確認済み＝エラーにはしない）
  - 重複時の上書き確認では、意味・補足をまとめて上書きするかを確認する（定着度・出題状況・スターは変更しない）

---

## 4. CSVインポートモーダル/画面

**目的**：CSVファイルから単語・意味（・補足）をまとめて登録する

**表示要素**
- ファイル選択（ドラッグ&ドロップ対応）
- インポート前のプレビュー（何件読み込めるか、重複語が何件あるか、エラー行の有無）
- 重複語が見つかった場合の処理方法の選択（インポート実行前に選ぶ）
  - 既存の意味・補足をそのまま残す（インポートデータは無視）
  - インポートデータの意味・補足で上書きする（意味・補足はまとめて1つのポリシーで上書き。片方だけ上書きという選択肢はない）
  - いずれの場合も、同じ単語は単語帳内に1件のみ残る
- インポート実行ボタン

**仕様（確認済み）**
- フォーマット：「単語,意味,補足」の2〜3列、ヘッダー行あり。補足列は省略可（2列のみでも読み込める）。3列目がある場合、値が空文字列でも構わない

**主な操作**
- ファイル選択 → パース → プレビュー確認（重複語の処理方法を選択）→ インポート実行
- インポート後は単語リスト画面に反映（新規単語は定着度「覚えていない」、出題状況は両方「未出題」、スターは「スターなし」で作成）

---

## 5. 出題前フィルタ・設定画面（フラッシュカード／4択クイズ共通パターン）

**目的**：これから学習するセッションの対象範囲・順番・問題数（枚数）を決める

**表示要素**（画面上から下の順）
- 出題順の選択：📋「登録順」または🔀「ランダム」のアイコンボタン（フラッシュカード・4択クイズ共通の設定。選択中は青く強調）
- 出題数（枚数）指定欄（ユーザーが数値を入力。対象単語数を超える値を指定した場合は、対象単語数に達した時点でセッションが終了する旨を明記）
- 絞り込み後の対象単語数のライブ表示（例：「対象単語：12個」）
- 絞り込み条件（3つのグループ、それぞれ複数選択可でAND組み合わせ）
  - 定着度（😞覚えていない／😐少し覚えた／😀覚えた）
  - スター（⭐スター付きのみ表示）
  - 出題状況（未出題／出題済）※フラッシュカード画面ではフラッシュカードの出題状況、4択クイズ画面では4択クイズの出題状況を参照する
- 「開始する」ボタン（画面右下寄せ）

**主な操作**
- 定着度・スター・出題状況を選択して対象単語数をリアルタイムに更新
- 出題順・出題数を入力（対象単語数が0件の場合は開始不可のガイド表示）
- 開始 → 対象単語からセッション内重複なしで出題キューを生成し、フラッシュカード出題画面 or 4択クイズ出題画面へ

---

## 6. 4択クイズ 出題画面

**目的**：4択問題を1問ずつ出題する

**前提**：この画面に遷移できるのは、単語帳の登録単語数（フィルタ前の総数）が4以上の場合のみ

**表示要素**
- 現在の単語（問題文）
- 4つの意味の選択肢（正解1つ＋誤答3つ。誤答は同じ単語帳内の全単語から重複なく抽出。選択肢の並び順はランダム）
- 現在の進捗（例：「3問目 / 10問」）
- スターのON/OFFトグルボタン（進捗表示と同じ行に配置。他の属性には影響しない）

**主な操作**
- 選択肢を選ぶ → 正誤判定（定着度更新・4択クイズ出題状況を「出題済」に）→ 即時フィードバック画面へ

---

## 7. 4択クイズ 即時フィードバック画面

**目的**：1問ごとの正誤と、各選択肢に対応する単語を確認する

**表示要素**
- 正解／不正解の表示
- 4つの選択肢（意味）それぞれに対応する単語を表示（正解の選択肢は強調表示）
- 更新後の定着度表示（アイコン＋ラベル、例：「更新後の定着度：😀 覚えた」）
- 「次の問題へ」ボタン（最終問題の場合は「結果を見る」ボタン）

**主な操作**
- 次の問題へ進む、またはセッションサマリー画面へ遷移

---

## 8. フラッシュカード 出題画面

**目的**：単語を見て意味を思い出し、めくって確認し、定着度を選択して次に進む

**表示要素（単語面・意味面共通のヘッダー）**
- 現在の進捗（例：「3枚目 / 10枚」）
- スターのON/OFFトグルボタン（他の属性には影響しない）
- セッションを中断して終了するボタン（押すとフラッシュカード終了画面へ。この場合も「出題範囲の定着度別件数」を表示する）

**表示要素（単語面）**
- 現在の単語
- タップ/クリックで意味面にめくる操作

**表示要素（意味面）**
- 意味
- **補足（登録されている場合のみ、意味の下に表示）**
- 現在の定着度表示
- 定着度選択ボタン（😞「覚えていない」😐「少し覚えた」😀「覚えた」の3つ、アイコン付き）。**いずれかを選択すると、その値が定着度に設定され、フラッシュカード出題状況が「出題済」になり、自動的に次の単語（単語面）へ進む**
- 最後の1枚で定着度を選択した場合は、次のカードへ進む代わりにフラッシュカード終了画面へ遷移する

**主な操作**
- カードをめくる → 意味と定着度選択ボタンを表示
- 定着度を選択 → 定着度・出題状況を更新し、次のカードへ（最後の場合は終了画面へ）

---

## 9. フラッシュカード終了画面

**目的**：セッションが終わったこと（または中断したこと）を伝え、成果を振り返る

**表示要素**
- 出題範囲（このセッションで対象とした単語群）における定着度の内訳：「覚えた ◯件／少し覚えた ◯件／覚えていない ◯件」
  - 最後まで進めた場合・途中で中断した場合のどちらでも、その時点での出題範囲全体の定着度を集計して表示する
- 「もう一度学習する」「単語帳に戻る（サブメニューへ）」ボタン

**主な操作**
- 同条件で再度学習を開始
- サブメニューまたは単語帳一覧に戻る

---

## 10. 4択クイズ セッションサマリー（リザルト）画面

**目的**：セッション全体の成績を振り返る

**表示要素**
- 総合成績（例：「10問中7問正解」「正答率70%」）
- 各問題の正誤一覧（単語・正誤・不正解の場合は正しい意味/単語を表示）
- 「もう一度学習する」「単語帳に戻る（サブメニューへ）」ボタン

**主な操作**
- 同条件で再度学習を開始
- サブメニューまたは単語帳一覧に戻る

---

## 画面遷移の全体フロー

サイドバーは学習セッション画面（フラッシュカード/4択クイズの出題〜終了・結果）以外の全画面に常設（詳細は `word-app-gui.md`）。

```
（初回起動のみ）アカウント作成画面
 │  はじめる → デフォルト単語帳「英検３級」を自動生成
 ↓
ホーム画面（★アプリのデフォルト画面。以降はここから開始）
 ├─ 使い方カードから各機能へ
 │
 ├─（サイドバー）プロフィール＝アカウント設定（ニックネーム・アイコン変更）
 │
 ├─（サイドバー）単語帳＝単語帳一覧
 │    └─ 単語帳を選択
 │         └─ サブメニュー（単語リスト／フラッシュカード／4択クイズ）
 │              ├─ 単語リスト
 │              │    ├─ 単語登録・編集（新規単語帳作成直後は自動でここが開いた状態）
 │              │    └─ CSVインポート
 │              │
 │              ├─ フラッシュカード → 出題前フィルタ・設定へ（下記と合流）
 │              └─ 4択クイズ（総単語数4以上の場合のみ選択可）→ 出題前フィルタ・設定へ（下記と合流）
 │
 ├─（サイドバー）フラッシュカード＝単語帳選択画面
 │    └─ 単語帳を選択
 │         └─ 出題前フィルタ・設定
 │              └─ フラッシュカード出題（単語面⇄意味面、定着度選択で次へ）
 │                   └─ フラッシュカード終了画面（中断時も同様に表示）
 │                        ├─ もう一度学習する（フィルタ・設定へ）
 │                        └─ サブメニューに戻る
 │
 └─（サイドバー）4択クイズ＝単語帳選択画面（総単語数4未満の単語帳は選択不可）
      └─ 単語帳を選択
           └─ 出題前フィルタ・設定
                └─ 4択クイズ出題 ⇄ 即時フィードバック（問題数分繰り返し）
                     └─ セッションサマリー
                          ├─ もう一度学習する（フィルタ・設定へ）
                          └─ サブメニューに戻る
```

---

## 決定事項の反映（更新履歴）

### 今回の更新分（サイドバー・ホーム画面・スター/補足属性・デフォルト単語帳）
- 画面左にサイドバーを追加（🏠ホーム／👤プロフィール／📚単語帳／🃏フラッシュカード／❓4択クイズ）。学習セッション画面以外の全画面に常設。スマホ幅では開閉式（メイン画面を押し出すプッシュ方式、オーバーレイで隠さない）
- ホーム画面を新設し、アプリのデフォルト画面にした（従来の単語帳一覧はサイドバーの「単語帳」経由に変更）。アップデートのお知らせと使い方チュートリアルを表示
- 上部ヘッダーに単語帳数・総単語数のリアルタイム統計を常時表示
- サイドバーの「フラッシュカード」「4択クイズ」から、単語帳選択画面経由で出題前フィルタ画面に直接入れるようにした（単語帳→サブメニュー経由の既存フローも維持）
- 単語・単語帳に「スター」属性を追加（定着度・出題状況とは独立。単語リスト・単語帳一覧・フラッシュカード/4択クイズ出題画面から切り替え可能。出題前フィルタの絞り込み条件にも使える）
- 単語に「補足」属性を追加（任意項目）。単語登録・編集モーダルで入力、単語リストにはアイコンのみ表示、フラッシュカードの意味面に表示、CSVインポートも3列目として対応
- 定着度・スター・出題順に絵文字アイコンを追加（詳細は `word-app-gui.md`）
- サブメニューの並び順を「単語リスト／フラッシュカード／4択クイズ」に変更（単語リストを先頭に）
- 単語帳の新規作成後、単語帳一覧に留まらず単語登録モーダルが開いた状態の単語リスト画面へ自動遷移するようにした
- 新規アカウント作成時に、チュートリアル・お試し用のデフォルト単語帳「英検３級」（100単語）を自動生成するようにした

### 前回の更新分（アカウント機能）
- アカウント（ニックネーム＋アイコン）の仕組みを追加。初回起動時にニックネームとアイコンを設定してアカウントを作成し、以降の単語帳データはこのアカウントに紐づけて保存する
- 1端末＝1アカウント固定とし、アカウントの追加作成・切り替え機能は持たない
- ニックネーム・アイコンは、初回作成後もアカウント設定画面から変更できる
- アイコンは「デフォルト（未設定）」「あらかじめ用意された動物イラスト」「カメラロールからのアップロード」の3パターンから選べる。アップロード画像は自動的に縮小・圧縮してから保存する
- 動物イラストの素材は、実装時にシンプルなイラスト／絵文字ベースで用意する想定
- データモデルにAccountエンティティを追加し、単語帳（Tanchou）にaccountId（FK）を持たせた

### さらに前の更新分（単語リスト画像の反映）
- 単語リストのイメージ画像を反映し、属性を「定着度（覚えた/少し覚えた/覚えていない、共通）」「フラッシュカード出題状況（出題済/未出題）」「4択クイズ出題状況（出題済/未出題）」の3つの独立した属性として整理（定着度に「未出題」という値は持たせない。新規登録時の定着度初期値は「覚えていない」）
- 単語リスト画面はこの3属性をそれぞれプルダウンで直接編集できる
- 出題前フィルタ・設定画面は「定着度」と「出題状況」の2軸を組み合わせて絞り込めるようにする
- 4択クイズが利用不可（総単語数4未満）の場合のメッセージ文言を確定：「4単語以上単語帳に登録することでこの機能は利用できるようになります。たくさん単語を登録しよう！」
- フラッシュカードの意味画面では、定着度ボタン（覚えた/少し覚えた/覚えていない）を選択する操作が「ラベル設定」と「次のカードへ進む」を兼ねる（別途「次へ」ボタンは置かない）
- フラッシュカードにセッション中断ボタンを設け、中断時も終了画面へ遷移する
- フラッシュカード終了画面は、正誤スコアではなく「出題範囲における定着度別の件数（覚えた/少し覚えた/覚えていない）」を表示する（中断時も同様）

### これまでの更新分
- フラッシュカード機能を追加（単語帳選択後に「フラッシュカード」「4択クイズ」「単語リスト」のサブメニューを表示）
- 単語編集時のラベルリセット：不要（そのまま保持）
- CSVインポートの重複語：そのまま残すか上書きするかをインポート時に選択（同じ単語は1件のみ）
- 単語帳削除時：確認ダイアログを表示する
- ラベル（定着度）遷移ルールを単純な状態遷移に整理（連続正解数カウンターは廃止。「少し覚えた」の状態で正解すれば「覚えた」になる）
- フラッシュカード・4択クイズの両方で、出題順（登録順／ランダム）を選択可能にする出題前設定画面を用意

---

## 要確認・仮決めのまま進めた点

（現時点で残っている確認事項はなし。ニックネームの文字数制限は1〜20文字で確定済み。絵文字・特殊記号も制限なく使用可）

---

## 将来の拡張構想（参考・当分先の予定）

現時点では実装しないが、将来的に以下の機能を追加したいという方針が共有されている。

- Googleアカウント等でのログイン機能
- 単語帳を全ユーザー、またはフレンドのみに公開する共有機能（イメージ：Apple Musicのプレイリスト共有）
- 単語帳への「いいね」機能
- ランキング・フレンド機能（イメージ：Duolingo）

MVP（最初のバージョン）はローカルのブラウザ内保存・単一ユーザーで作るが、上記を見据えて次の点を意識しておくと、あとで作り直しになりにくい。

- **データアクセス層を抽象化しておく**：単語帳・単語のCRUDを直接localStorageに触るのではなく、`wordbookRepository`のような関数群/インターフェース越しに行う設計にしておく。将来、実装をlocalStorage版からAPI（バックエンド）呼び出し版に差し替えやすくなる。
- **アカウントの仕組みはすでにMVPに組み込み済み**：ニックネーム＋アイコンで作成するローカルアカウント（accountId）と、単語帳への紐付けはすでに実装スコープに入っている。将来Googleログイン等を導入する際は、このローカルアカウントをサーバー側のユーザーIDに紐付ける（あるいはログインを機にマージする）移行処理を想定する。
- **将来の項目を見越したデータ構造にしておく**：例えば単語帳に`visibility`（`private`固定でよい。将来`public`/`friends`を追加）といったフィールドを最初から持たせておくと、あとからの移行が楽になる。ただしUI上はこれらを一切見せない・触らせない。
- **認証・共有・いいね・ランキングを実装する段階になったら、バックエンド導入が必要**：候補としては以下のようなものがある（いずれも今すぐ決める必要はない）。
  - **Firebase**：Google製で、Googleログインとの相性が良い。Firestore（NoSQL）、認証、ホスティングが一体で個人開発によく使われる。
  - **Supabase**：PostgreSQL（SQL）ベースでOSS。GoogleなどのOAuthにも対応し、SQLでランキング集計などがしやすい。セルフホストの選択肢もある。
  - 自前のNode.js等のバックエンド＋DBを構築する方法もあるが、認証やリアルタイム同期を自前実装する分、開発コストは上がる。

このセクションはあくまで今後の方針メモであり、MVPの実装スコープには含めない。
