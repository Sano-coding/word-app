# 単語暗記アプリ GUIルール（最新版）

`word-app-screens_1.md`（初期の画面設計）をベースに実装を進める中で、チャット上のやり取りを通じて画面構成・ナビゲーション・表示ルールに多くの変更が加わった。このドキュメントは、それらの変更を反映した**現時点でのGUIの正式なルール**をまとめたもの。個々の画面の目的・表示要素・操作の詳細は `word-app-screens_1.md` を参照し、本ドキュメントは全体のナビゲーション構造・共通レイアウト・アイコン規則など「GUIとして横断的に効いてくるルール」を扱う。

---

## 1. 全体レイアウト

画面は大きく「左サイドバー」＋「メインエリア（上部ヘッダー＋コンテンツ）」の2カラム構成。

```
┌─────────────┬──────────────────────────────┐
│             │  ☰（スマホ幅のみ） 単語帳   📚1冊｜🔤N語 │ ← 上部ヘッダー（常時表示・sticky）
│  サイドバー   ├──────────────────────────────┤
│             │                              │
│             │      各画面のコンテンツ         │
│             │                              │
└─────────────┴──────────────────────────────┘
```

- サイドバー・上部ヘッダーは**学習セッション画面（フラッシュカード出題／終了、4択クイズ出題／結果）では非表示**。それ以外の全画面（ホーム・プロフィール・単語帳一覧・サブメニュー・単語リスト・出題前フィルタ）では表示される
- ログイン／新規登録画面（未ログイン時）、アカウント作成画面（ログイン済みだがプロフィール未作成の場合）はサイドバー・ヘッダーの対象外

### サイドバーの開閉（オーバーレイ方式、2026-09-22更新）

- 768px以上（PC幅）：常時表示（固定カラム、幅220px）。開閉ボタンは出さない。メイン画面との関係はこれまでと同じ
- 768px未満（スマホ幅）：既定では非表示。上部ヘッダーの☰ボタンで開く
  - **メイン画面のサイズは変わらず、サイドバーがメイン画面に重なる形で表示される「オーバーレイ」方式**。開いている間はメイン画面全体が半透明の黒背景で暗くなる
  - 開く操作：上部ヘッダーの☰ボタン（**開く専用**。開閉トグルではない）
  - 閉じる操作：サイドバー内の見出し行（アイコン・ニックネームの右）にある「＜＜」ボタン、暗くなった背景部分をタップ、またはサイドバーのナビ項目をクリックして別画面へ遷移したとき（自動で閉じる）
  - **ホーム画面（`/`）に来たとき（初回表示・遷移とも）は、PC幅ではサイドバーが自動的に開いた状態になる。スマホ幅ではホーム画面でも既定で閉じた状態のまま**

### 上部ヘッダー

- 左：ハンバーガーボタン（スマホ幅のみ）＋アプリ名「単語帳」
- 右：現在のアカウントの統計を常に表示（画面遷移なしでもリアルタイム更新）
  - 📚 単語帳の数（例：`1冊`）
  - 🔤 総登録単語数（例：`108語`）
  - 単語帳・単語の作成／削除／CSV一括インポートの直後に自動更新される

---

## 2. サイドバーのナビゲーション項目

上から順に以下の5項目。各項目は絵文字アイコン＋ラベルで、現在地に応じてハイライトされる。

| 順番 | アイコン | ラベル | 遷移先 | アクティブ判定 |
|---|---|---|---|---|
| 1 | 🏠 | ホーム | `/` | パスが `/` と一致 |
| 2 | 👤 | プロフィール | `/settings` | パスが `/settings` と一致 |
| 3 | 📚 | 単語帳 | `/wordbooks` | パスが `/wordbooks`、または `/tanchou/:id`、`/tanchou/:id/words` |
| 4 | 🃏 | フラッシュカード | `/flashcards` | パスが `/flashcards`、または `/tanchou/:id/flashcard` |
| 5 | ❓ | 4択クイズ | `/quiz` | パスが `/quiz`、または `/tanchou/:id/quiz` |

サイドバー上部には現在のアカウントのアイコン・ニックネームを表示し、クリック/タップするとプロフィール画面（`/settings`）へ遷移する。

### 「フラッシュカード」「4択クイズ」の直接起動フロー

サイドバーからフラッシュカード／4択クイズを押すと、単語帳選択専用の一覧画面（`TanchouSelectPage`）が表示される。単語帳を選ぶと、従来の「単語帳→サブメニュー→フラッシュカード/4択クイズ」と同じ出題前フィルタ画面に直接進む（画面自体は共通、経路が増えただけ）。

- 単語帳から直接（従来どおり）：単語帳を選ぶ → サブメニュー（単語リスト／フラッシュカード／4択クイズ）→ 各機能
- サイドバーから直接：「フラッシュカード」または「4択クイズ」を押す → 単語帳選択画面 → 各機能の出題前フィルタ画面
  - 4択クイズの単語帳選択画面でも、総単語数4未満の単語帳は選択不可＋案内文言を表示（サブメニューと同じ制御）

---

## 3. ホーム画面（`/`、アプリのデフォルト画面）

- **アカウント作成直後・以降の再訪問とも、`/` がデフォルトの着地画面**（旧仕様では単語帳一覧がデフォルトだったが、単語帳一覧は `/wordbooks` に移動した）
- 表示内容：
  1. （該当する場合のみ）**ローカルデータのクラウド移行バナー**：Supabase移行前にその端末のlocalStorageへ保存されていたデータ（単語帳・単語）が残っている場合に表示。「アップロードする」（進捗込みで現在ログイン中のアカウントに複製）／「しない」の2択ボタン。いずれかを選ぶと以後は再表示しない（詳細は `word-app-screens_1.md` の「0. ホーム画面」を参照）
  2. 見出し「ホーム」
  3. 「アップデートのお知らせ」：直近の更新点を箇条書きで表示
  4. 「使い方」：3つのチュートリアルカード（タップで該当機能へ遷移）
     - 📚「単語帳」から覚えたい単語とその意味を登録しよう！　→ 単語帳一覧へ（サブテキスト：「単語リストの単語を押すと意味と補足を編集できるよ」）
     - 🃏「フラッシュカード」で単語の意味と定着度を確認しよう！　→ フラッシュカード用単語帳選択へ
     - ❓「４択クイズ」で効率よく単語の意味を覚えよう！　→ 4択クイズ用単語帳選択へ

---

## 4. アイコン規則

絵文字ベースで統一。同じ意味は同じ絵文字を使う。

| 対象 | アイコン |
|---|---|
| 定着度：覚えていない | 😞 |
| 定着度：少し覚えた | 😐 |
| 定着度：覚えた | 😀 |
| スター | ⭐（付き）／☆（なし） |
| 単語の補足あり／なし（単語リスト列） | 📝／— |
| 出題順：登録順 | 📋 |
| 出題順：ランダム | 🔀 |
| サブメニュー：単語リスト | 📋 |
| サブメニュー：フラッシュカード | 🃏 |
| サブメニュー：4択クイズ | ❓ |
| 単語帳一覧カード：編集（名称変更） | ✏️ |
| 単語帳一覧カード：削除 | 🗑️ |
| 単語帳一覧カード：ドラッグハンドル（並び替え） | ⠿ |

定着度アイコンは `src/domain/labels.ts` の `MASTERY_LEVEL_ICONS` に集約されており、出題前フィルタ画面のチェックボックス・フラッシュカードの定着度選択ボタン・4択クイズの即時フィードバック画面すべてで共通利用される。

単語帳一覧カードのドラッグハンドル（⠿）を長押し・ドラッグすると単語帳の表示順を入れ替えられる（カード本体をタップすると単語帳が開くため、並び替え操作はハンドル部分に限定している）。並び順は`tanchous.sort_order`としてSupabaseに保存され、他端末にも同期する。フラッシュカード／4択クイズの単語帳選択画面（`TanchouSelectPage`）も同じ並び順を参照する。

選択中であることを示す強調色は青（`--color-primary`／背景 `--color-primary-muted`）で統一（出題順ボタン、サイドバーのアクティブ項目など）。

---

## 5. 単語・単語帳の追加属性とその表示

初期仕様（単語・意味・定着度・フラッシュカード出題状況・4択クイズ出題状況）に加えて、以下を追加：

### スター（単語・単語帳の両方）

- `isStarred: boolean`。定着度・出題状況とは独立した属性
- 単語：単語リストのテーブル列（⭐/☆トグル）、フラッシュカード出題画面・4択クイズ出題画面のヘッダーにトグルボタンとして表示
- 単語帳：単語帳一覧のカードにトグルボタンとして表示
- 出題前フィルタ画面で「スター付きのみ表示」として絞り込み条件に使える（定着度・出題状況の絞り込みとAND条件で組み合わせ可能）

### 補足（単語のみ、任意項目）

- `note: string`（空文字列＝補足なし）
- 単語の登録・編集モーダルに複数行の入力欄（任意、空でも保存可）
- 単語リストのテーブルに列として表示（内容は表示せず📝/—のアイコンのみ。📝をタップ/クリックするとその行の補足内容をポップアップ表示し、もう一度アイコンを押す、またはポップアップ自体を押すと閉じる。編集は単語名クリックで開く登録・編集モーダルから）
- **フラッシュカードの意味面（めくった後の画面）に、補足があれば意味の下に表示する**
- CSVインポートで「単語,意味,補足」の2〜3列（補足列は省略可）に対応。重複語の上書きポリシーは意味・補足をまとめて上書きする

---

## 6. 単語帳（サブメニュー）画面のレイアウト

単語帳を選んだ後のサブメニュー項目の並び順：

1. 📋 単語リスト（先頭）
2. 🃏 フラッシュカード
3. ❓ 4択クイズ（総単語数4未満の場合は選択不可＋案内文言）

各項目の左にアイコンを表示。

---

## 7. 出題前フィルタ画面のレイアウト

フラッシュカード・4択クイズ共通の画面（`PreSessionFilterPage`）。上から順に：

1. 出題順（📋登録順／🔀ランダムのアイコンボタン、選択中は青く強調）
2. 出題数（数値入力）
3. 対象単語数のライブ表示
4. 絞り込み条件（定着度／スター／出題状況の3つのfieldset）
5. 「開始する」ボタン（画面右下寄せ）

絞り込み条件の各選択肢は、チェックボックス＋ラベルではなく**出題順ボタンと同じトグルボタン（チップ）形式**で表示する。押すと選択され青く強調表示、もう一度押すと選択解除されて通常表示に戻る（定着度・出題状況は複数選択可、スターは単一トグル）。

---

## 8. 単語帳の新規作成フロー

単語帳一覧で「+ 新規作成」→名前を入力して保存すると、単語帳一覧に留まらず、作成した単語帳の単語リスト画面へ自動遷移し、単語登録モーダルが開いた状態になる（1つ目の単語をすぐ入力できる）。

---

## 9. 新規アカウント作成時のデフォルト単語帳

新規アカウント作成のたびに、チュートリアル・お試し用の単語帳「英検３級」（単語100件、意味・品詞の補足つき）が自動生成される。内容は `src/domain/seedData.ts` に同梱されており、元データは `英検３級　単語リスト - シート1.csv`。通常の単語帳と同じく自由に編集・削除できる。

---

## 10. 学習セッション画面（フラッシュカード／4択クイズ）の共通ルール

サイドバー・上部ヘッダーが非表示になる出題中・結果画面群には、以下の共通ルールがある。

### ヘッダー（出題中の画面のみ）

進捗表示（「3枚目/10枚」「3問目/10問」）の右に、次の2つのボタンを横並びで配置する：

1. ⭐/☆ スターのON/OFFトグルボタン（現在の単語のスターを切り替える。他の属性には影響しない）
2. 「中断する」ボタン（フラッシュカード・4択クイズの両方に存在）。押すと即座に終了画面／セッションサマリー画面へ遷移し、**その時点までに回答・確認済みの単語のみ**を対象に結果を集計する（未回答分は含めない）

### アクションボタンの配置

出題前フィルタ画面の「開始する」、4択クイズ即時フィードバック画面の「次の問題へ」／「結果を見る」は、いずれも**画面右下寄せ**で統一する。

### フラッシュカード出題画面：タップ範囲の拡張とボタンの下部固定（2026-09-23）

カードが画面上部にありスマホで押しづらいという課題への対応として、フラッシュカード出題画面のみ以下のルールを追加する（4択クイズは対象外）。

- 単語面では、案内文言を「タップして意味を確認」に変更し、**カード本体だけでなくカード下の余白（ページ下端まで）をタップしても意味面にめくれる**ようにする
- 意味面では、「現在の定着度」表示をカードの外・下に移動し、定着度選択ボタン（😞😐😀の3つ）は**画面下端に固定表示**する（スクロールしても常に押せる位置に留まる）
- 意味面のカード左上に「← 単語に戻る」ボタンを設置し、押すと単語面に戻れる（進捗・定着度は変更しない）。単語は意味面に常時表示せず、この戻るボタン経由でのみ再確認できるようにする（想起練習の負荷を保つための意図的な設計。詳細は `word-app-screens_1.md` の「8. フラッシュカード 出題画面」を参照）

---

## 11. ログイン／新規登録画面のルール

- 見出しは📚アイコン＋「単語帳」（アイコンは単語帳ナビと同じ絵文字で統一）
- リード文中の該当アクション語（ログインモードなら「ログイン」、新規登録モードなら「新規登録」）に下線を表示する
- **色は「ボタンの役割」ではなく「対応するアクション」で統一する**：ログインにつながるボタン（送信ボタン・モード切り替えボタンとも）は青（`--color-primary`）、新規登録につながるボタンはピンク（新設の `--color-accent`、Buttonコンポーネントの`accent`バリアント）。そのため送信ボタンとモード切り替えボタンの色は、現在のモードに応じて入れ替わる（ログインモード時：送信＝青／切替＝ピンク、新規登録モード時：送信＝ピンク／切替＝青）
- サインアップ後にメール確認が必要な場合（Supabaseのデフォルト設定）は「確認メールを送信しました。メール内のリンクを開いてからログインしてください。」を表示する

---

## 12. データ保存先とマルチデバイス対応

- データはSupabase（PostgreSQL）に保存され、`profiles`（Account）／`tanchous`（Tanchou）／`words`（Word）の3テーブルと、アカウントごとに読み書きを分離するRow Level Securityポリシーで構成される
- 認証はメールアドレス＋パスワード（Supabase Auth）。同じアカウントであれば、PC・スマホなど複数端末から同じ単語帳データにアクセスできる（1端末＝1アカウント固定だった旧仕様は廃止）
- アプリ本体はGitHub Pages（`https://sano-coding.github.io/word-app/`）で公開。`index.html`は`lang="ja"`＋`notranslate`指定とし、英単語混在ページがブラウザの自動翻訳機能で意図せず書き換えられないようにしている

---

## 13. 戻るボタンの表記統一

各画面上部の「1つ前の画面へ戻る」ボタンは、遷移先によらずすべて「← 戻る」で統一する（旧仕様では「← サブメニューへ」「← 単語帳一覧へ」など遷移先名を含めていたが、表記をシンプルに統一した）。対象：単語リスト画面、出題前フィルタ・設定画面、単語帳サブメニュー画面。

---

## 14. 単語リストの表：先頭列（単語）の固定表示

単語リストの表はスマホ幅で横スクロールが発生するが、スクロールしても「単語」列だけは画面左に固定表示される（`position: sticky`によるスプレッドシートの「ウィンドウ枠の固定」相当の挙動）。「意味」「補足」などの列を確認するために右へスクロールしても、どの単語の行か見失わない。

- 単語が長い場合、固定列の幅が際限なく広がらないよう「単語」ボタンは最大幅120pxで省略表示（「…」）する。省略された場合も、その単語をタップ/クリックすれば編集モーダルが開き全文を確認・編集できる（PCではホバーでも全文をツールチップ表示）
