# 今後の展望

MVP（ローカル保存・単一ユーザー）の次の段階として検討している拡張機能のメモ。`word-app-screens_1.md` の「将来の拡張構想」で触れていたフレンド機能・共有機能・いいね機能・ランキング機能を具体化したもの。

> **前提**：これらの機能はすべてバックエンド・認証基盤の導入が必要（ローカルのアカウント同士がフレンドになる、単語帳を他人と共有する、という時点でサーバーが必須）。候補は `word-app-screens_1.md` に記載のとおり Firebase／Supabase など。本ドキュメントは機能仕様のメモであり、着手時期・技術選定はここでは決めない。
>
> Tanchou（単語帳）には将来の公開/非公開切り替えを見据えて `visibility`（現状は常に `'private'`、UI非表示）を予約済み。「サーチ機能といいねの実装」の公開/非公開設定はこのフィールドを使う想定。

---

## フレンド機能の実装

- サイドバーに「フレンド」メニューをアイコン付きで追加し、自分のフレンド一覧をリスト表示する。
- フレンドになるには、自分から「申請」→相手が「承認」する双方向の同意が必要。相手からすでに申請が来ている場合は、自分は「承認」を押すだけでよい（申請は不要）。
- フレンドはアカウントID（完全一致）で検索する。自分のアカウントIDはプロフィール画面に表示する。
- フレンド一覧からフレンドを削除できる。削除は双方向（自分の一覧から削除すると、相手の一覧からも自動的に消える）。
- フレンドの単語帳は、フラッシュカード・4択クイズともにプレイできる。
  - 単語リストの属性変更（定着度・出題状況の手動変更）やスター付けはできない。単語帳所有者側のデータには一切影響しない。
  - 出題前フィルタは「出題数」「出題順（登録順／ランダム）」のみ選択可能。定着度・出題状況・スターによる絞り込みはできない（フレンドの単語帳の学習状況を細かく覗き見る形にはしない）。
  - プレイした側（自分）には成績・exp獲得が記録される（詳細はランキング機能を参照）。

---

## サーチ機能と「いいね」の実装

- 自分の単語帳ごとに「公開する」／「非公開にする」を選べる。デフォルトは非公開。プロフィール画面で、新規作成時のデフォルト公開設定を変更できる。
- メニューに「サーチ」をアイコン付きで追加する。
- サーチ画面では、公開されている単語帳を名前（部分一致）で検索できる。検索結果から単語帳を選ぶと、デモプレイ（フラッシュカード／4択クイズ、出題数10問固定・ランダム順）ができる。デモプレイも、フレンドの単語帳と同様に所有者側のデータには影響せず、プレイした側に成績・exp獲得が記録される。
- 検索で見つけた（公開されている）単語帳には「いいね」（ハートアイコン）ができ、単語帳にいいね数が表示される。「いいね」は1ユーザー1単語帳につき1回のトグル式（もう一度押すと解除できる）。

---

## ランキング機能の実装

### 単語帳ランキング

- 公開されている単語帳を、いいね数でランキング表示する。
- 「全体」「フレンド」を切り替えられる。

### ユーザーランキング

- 単語帳の作成、単語の登録、フラッシュカードのプレイ、4択クイズのプレイでexp（経験値）を獲得する。フレンド・サーチ経由で他人の単語帳をプレイした場合も、プレイした側はexpを獲得する（単語帳所有者側のデータには影響しない）。
- プロフィール画面に、総獲得exp・週間exp（直近7日間の獲得分）を表示する。
- ユーザーランキングは「総獲得ランキング」「週間ランキング」の2種類があり、それぞれ「全体」「フレンド」を切り替えられる。

---

## スマートフォン版アプリの作成

- Webアプリと同じ機能・内容のまま、ナビゲーションUIのみスマートフォン向けに変更した別のアプリを作る。
  - Web版の「スマホ幅ブラウザでの表示」（ハンバーガーボタン＋プッシュ方式サイドバー）とは別物。Web版のスマホ幅表示は今のままでよく、今回のUIはスマホアプリ専用として新設する。
- 画面上部：
  - 左：ユーザーアイコン。その右にホーム・プロフィールをアイコンのみ（ラベルなし）で表示する。
  - 右：作成済み単語帳数・総登録単語数を表示する（Web版の上部ヘッダー統計に相当）。
- 画面下部：単語帳・フラッシュカード・4択クイズ・サーチ・フレンド・ランキングの6項目を、アイコンのみのボトムタブバーとして並べる（サーチ・フレンド・ランキングは、それらの機能が実装されてから追加する）。

---

## 要検討事項（次のステップで詰める）

以下は今回の確認で方針が決まったものの、実装時に具体的な設計が必要な点です。

- **exp不正対策**：フレンド・サーチ経由のプレイでもexpが入る仕様にしたため、同じ単語帳（特に他人の単語帳）を何度も周回してexpを稼ぐ、といった水増し行為への対策が必要になる。例：同一単語帳からのexp獲得は1日1回まで、プレイごとのexp量を抑える、など。
- **exp付与量の設計**：単語帳作成・単語登録・フラッシュカードプレイ・4択クイズプレイそれぞれ何exp入るかは未設定。行動ごとの重み付けを別途決める。
- **いいね済み一覧**：自分が「いいね」した単語帳の一覧を見られる画面を用意するかは未定（あると便利だが必須ではない）。
- **スマホ版アプリの実装方式**：React Native等で別コードベースを作るか、既存のWebアプリをCapacitor等でラップするか、PWA化するかは未定。着手時に決める。
